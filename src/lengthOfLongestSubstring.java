import java.util.HashSet;

public class lengthOfLongestSubstring {

    /*
    *Input: String
    * Output: Int representing length of longest possible substring
    * with no repeating characters
     */
    public int lengthOfSubstring(String s){

        // use a hash map and two pointers
        int i = 0;
        int j = 0;
        int max = 0;

        HashSet<Character> hash_set = new HashSet<>();

        return 0;
    }
}
